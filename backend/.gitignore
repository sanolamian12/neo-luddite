# secrets & local env — never commit the Upstage key
.env
.env.local

# python
__pycache__/
*.pyc
.venv/
venv/

# 논문 실험 (답변 원문·KB 발췌 포함 — 커밋 금지)
testset.json
answers_long.csv
smoke.csv

# 실행 로그
*.log
