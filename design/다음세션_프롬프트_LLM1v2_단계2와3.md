✅ **단계 0.5·1 은 2026-09-25 에 끝났다** — 서버에 꺼진 v2 유닛 + Caddy 폴백, 브랜치 `agentic-v2` 에 골격과 `?pipeline=v2` 스위치. **O-2 확정.**
이 프롬프트가 다음 **제품 세션**의 것이다.

**먼저 읽는다 (순서대로)**
1. `design/LLM1v2_역할축소_구현설계.md` — 설계 마스터. **§3 흐름 · §4-1 서버 위상 · §6 O-2(askState/action 모양 + Zod 함정) · §7 측정 밖 변경 · 부록 A·B 원문**
2. `history/260925_LLM1v2_단계0.5와1_…O2확정.md` — 직전 세션(설계가 틀렸던 것 네 가지)
3. `docs/doing/BEFORE_AFTER_기능명세.md` A1·A1-1 · `docs/doing/260923_LLM1데이터셋_검토/회신_정책확정_260924.md`(D3·D4 명세)
4. `docs/doing/v2격리_원복설계.md` §2(클론 범위)·§5(부딪히는 자리)

**작업 브랜치: `agentic-v2`** (`git switch agentic-v2`). 서버 v2 코드는 `/opt/neo-luddite-v2` worktree — deploy.sh 가 안 만진다.

═══ 범위: 설계 §5 의 단계 2 와 3 ═══
**2 — W3 `llm1.extract_facts()`**
- 새 파일 `backend/api/llm1.py`. 프롬프트는 **부록 A 원문 그대로**(`sufficiency` 필드 포함, **값은 무시** — §7-1). `backend/api/prompts/v2/` 에 둔다.
- `llm.py` 의 `bounded_client`·`_history_to_messages` 재사용(`llm.py` 수정 금지). 호출 규약: **`tool_choice` 강제 · `max_tokens`(600) · temperature 0 · `upstage_gate` 경유.**
- `engine_adapter` 지출유형 enum 을 참조하지 않는다. 완료 조건: 되묻기 갈래가 엔진 enum 을 안 봄 · 병의원 회귀 케이스 유지.

**3 — W4 축소판 (흐름 + D4 + 대조 + 건너뛰기)**
- `pipeline_agentic.run_clinic` 을 §3 흐름으로: 1턴 추출 → ask(상위 2)/proceed · 2턴 `check_asked()`(부록 B, 문구는 "1턴에 물은 사실"로 개작 — §7-2)
  · D3 대체 질문 1회 · D4 상한 → `proceed_with_gap`. `is_stalled` 집계에서 v2 되묻기 턴 **제외**(호출부에서, `handoff.py` 무변경).
- **O-2 구현**: `schema.py` 에 `AskedFact`·`AskState`·`Message.askState?`·`ChatRequest.action?` 선택 필드 —
  ⚠️ **같은 커밋에서** `frontend/lib/conversation-schema.ts` `messageSchema` 에도 `askState` 선택 필드. 안 하면 Zod 가 조용히 지워 2턴에 상태가 없다.
- 프론트(브랜치): 되묻기 카드에 **"이대로 답변 받기"** 버튼 → `send({ text: "이대로 답변 받기", action: "proceed_with_gap" })` · `services/chat.ts` 가 `?pipeline=v2` 를
  붙이는 스위치(예: `NEXT_PUBLIC_CHAT_PIPELINE`, Vercel **Preview** 환경에만).
- `rag.chat_turns` 에 **v2 전용 outcome 값**(지금 골격은 v1 값으로 기록된다 — 격리 설계 §5). 새 값이 CHECK 제약에 걸리면 **확장만**, 마이그레이션은 적용 전 사용자 확인.
- 완료 조건: **3턴 시나리오(부분답변·모르겠어요·상한) + 건너뛰기 버튼** 로컬 통과.

═══ 재논의 금지 ═══
- LLM1 은 1턴에서 충분을 판정하지 않는다 · D4 는 로직 · 2턴 대조 범위는 닫는다(열린 재추출 금지).
- 모델 교체 · 검색형 체크리스트 RAG · D3·D4 값 · action 3종 · KB2 폐기 · solar-pro3 · **O-1·O-2(9/25 확정)**.

═══ 하지 말 것 ═══
- `import-credigraph`·`main` 에 v2 코드 push · 프론트 `main` push.
- v2 유닛 enable(단계 5) · 유닛의 `BACKGROUND_JOBS=off` 제거(리퍼가 v1 job 을 죽인다).
- 기존 테이블 ALTER/DROP · `kb2.*` 삭제 · 공유 파일 클론 · `pipeline.py`·`llm.py`·`handoff.py` 수정.
- 로컬 스모크에서 프로덕션 DB 에 쓰기 — `SUPABASE_DB_URL=""` 로 띄운다(dotenv 는 기존 키를 덮지 않는다).
- 데이터·평가(턴 단위 평가 = 단계 4)는 별도 데이터 세션.

═══ 세션 마무리 ═══
1. 설계 마스터 § 진행 기록 + 어긋난 본문 수정. 2. `history/2609XX_LLM1v2_…md`. 3. 메모리 `project_llm1_pivot`.
4. 다음 프롬프트를 `design/` 에 새로 쓰고 **이 파일은 지운다**. 5. 문서는 import-credigraph 에 커밋 → `agentic-v2` 로 merge(반대 방향 금지).
