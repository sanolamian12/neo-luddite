# design/ — 여러 세션에 걸친 공사의 설계 마스터

이 저장소는 원래 코드(`backend/`, `frontend/`, `supabase/`)와 기록(`history/`)만 추적하고
`docs/` 는 로컬 전용이다. 그런데 한 기능을 여러 세션에 나눠 구현할 때는 **설계 마스터와
다음 세션 인수인계 프롬프트가 기기에 묶여 있으면 안 된다** — PC를 옮기거나 클론을 새로 뜨면
사슬이 끊기고, 다음 세션이 설계를 처음부터 다시 지어낸다.

그래서 그 두 종류만 여기에 둔다(2026-09-21).

| 넣는 것 | 넣지 않는 것 |
|---|---|
| 설계 마스터(`*_설계.md`) — 결정·데이터 모델·단계표·진행 기록 | 접속 정보·키·`.env` 값 |
| 세션 인수인계 프롬프트(`다음세션_프롬프트_*.md`) | 실 고객 자료, 대화·답변 원문 데이터 |
| | 완료 후 아카이브 — 끝난 공사는 `history/` 요약으로 남기고 여기서 지운다 |

데모 계정(`<id>@demo.local` / `demo1234`)은 이미 `supabase/seed` 와 e2e 스펙에 있으므로 무방하다.

## 규칙

- **진행 기록은 설계 문서 안(§ 진행 기록)에 쌓는다.** 단계가 끝날 때마다 커밋한다.
- **설계가 현실과 어긋났으면 본문을 고친다.** 기록만 덧붙이고 본문을 두면 다음 세션이 옛 설계를 믿는다.
- 각 세션은 끝나면서 **다음 단계 프롬프트 파일을 여기에 새로 쓴다.** 그게 사슬이다.
- `docs/doing/` 은 그대로 로컬 작업 공간이다(측정 자료, PDF, 팀 공유본 등).

## 지금 있는 것

- `세무사연결_핸드오프_이식설계.md` — 선행 기능 (a)(b)(c) 배포 완료
- `LLM1v2_역할축소_구현설계.md` — LLM1 v2(추출·되묻기·2턴 닫힌 대조, 멈춤=D4 로직) 구현 공사. 착수 2026-09-25, 단계 0.5·1·2·3 완료(9/25, 코드는 브랜치 `agentic-v2`), 다음 = `다음세션_프롬프트_LLM1v2_단계4와5.md`(4=데이터 세션 먼저)
- `KB3_요지수집_설계.md` — LLM2 용 KB3: 국세법령정보시스템 판례 **요지** 수집(law.go.kr 검색 → 302 로 문서 ID → taxlaw 요지). 착수 2026-09-25(경로 실측·설계, 코드 0), 다음 = `다음세션_프롬프트_KB3요지수집_파일럿.md`(데이터 세션, **LLM1 단계 4 보다 먼저 권장** — 리드타임이 가장 길다)

## 접은 것

- `인앱상담사채팅_상담사풀_설계.md` — **공사 전체 종결(2026-09-23)**, (a)~(e) + 후속 1~4단계 전부 배포·적용.
  약속대로 **history/ 요약만 남기고 여기서 지웠다** → `history/260923_인앱상담사채팅후속4_컬렉션채널하나로_준비대기는바인딩당_공사전체종결.md`.
  그 요약이 결산표·Realtime 함정 다섯·작업 환경 메모를 들고 있으므로 다음에 이 근처를 건드릴 때는 그것부터 읽는다.
  설계 전문은 git 이력에 있다(마지막 개정 `933cb52`). 단계별 기록은 `history/2609*_인앱상담사채팅*.md` 아홉 편.
  지시서(`다음세션_프롬프트_상담사채팅*`)는 단계가 끝날 때마다 지웠다 — 역시 git 이력에 있다.
