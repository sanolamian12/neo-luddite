# 인앱 상담사 채팅 · 비식별 상담사 풀 — 설계

- 작성: 2026-09-21 (설계만. **구현은 새 세션**에서 단계별로)
- 진행: **(a) 완료·배포(2026-09-21, 0037)** → 다음 (b). 상세는 §11
- 선행 설계: `design/세무사연결_핸드오프_이식설계.md` — (a) 카드·신청 / (b) 받는 쪽 3화면 / (c) Realtime 유실·프로필 사진
- 현행 한계: 지금은 세무사가 신청을 수락하면 **연락처(전화·이메일·카카오)가 열리고 앱 밖에서** 이어진다.
  ① 고객이 고른 세무사가 거절하면 흐름이 끊기고 ② 대화를 볼 수 있었다면 손을 들었을 다른 세무사에게 닿을 길이 없다.
- 이 설계가 여는 것: **앱 안 1:1 채팅방** + **비식별 사례 풀**(고객이 고르지 않은 세무사도 사례를 보고 먼저 손을 들 수 있다).

---

## 1. 목표 흐름

```
[경로 A — 고객이 고른다]                    [경로 B — 풀에서 손을 든다]

AI 챗 중 연결 트리거(기존 (a))
  ↓
세무사 카드 목록에서 1명 선택
  ↓
③ 동의 시트: "이 대화를 비식별 처리해
   상담사 풀에 올려도 될까요?"  ──────────→  ⑤ 풀에 비식별 사례 등재(7일)
  │  (동의 없이도 ④는 진행된다)                        ↓
  ↓                                          ⑦ 풀을 본 세무사가 연결 요청
④ 신청(pending) → 그 세무사에게 알림              (프로필 + 메시지 1건 동봉)
  ↓                                                  ↓
⑥ 세무사 수락                                 ⑧ 고객이 설정 메뉴에서 확인
  ↓                                             → 승인 / 거절
  └──────────────→ 채팅방 개설 ←──────────────────────┘
                      ↓
     ⑨ 같은 대화에 대해 방이 여럿일 수 있다(세무사별 1:1, 상한 3)
```

- **경로 A의 동의는 선택**이다. 동의를 거절해도 고른 세무사에게 가는 신청(④)은 그대로 진행된다.
  동의는 "내가 고르지 않은 세무사에게도 보여도 되는가"에 대한 답이지, 신청의 전제 조건이 아니다.
- **채팅방은 두 경로의 합류점**이다. 경로 A의 수락(⑥)과 경로 B의 승인(⑧)이 같은 방을 연다.
- 방은 **대화(conversation) 단위가 아니라 (대화, 세무사) 쌍 단위**다. 한 대화에 세무사 3명이 붙으면 1:1 방이 3개다.

---

## 2. 결정 사항 (2026-09-21 사용자)

| # | 결정 | 근거 |
|---|---|---|
| **D1** | 비식별화 = **규칙 마스킹 + 원문 그대로** | 결정적이라 재현·검증이 되고 Upstage 호출 0회. LLM 재작성은 날조 수치 위험이 있고, 세무사가 보는 것이 원문이 아니게 된다 |
| **D2** | 세무사의 **원문 열람 범위를 이번에 같이 좁힌다** | 아래 §3.7. 안 좁히면 "비식별 풀"이 화면 관행일 뿐 강제가 아니다 |
| **D3** | 경로 A도 **채팅방으로 일원화**. 연락처 공개는 부가 정보로 남긴다 | "어디서 이야기하나"가 둘로 갈리지 않는다 |
| **D4** | 풀 노출 동의 = **대화 1건 · 7일 만료 · 언제든 철회** | 철회해도 이미 열린 방은 유지(대화 중인 상대를 끊지 않는다) |

### D2 의 근거가 된 실측 (2026-09-21)

세무사 계정 토큰으로 PostgREST 직접 질의 → **일감 배정과 무관하게 전체 대화 218건이 payload 째 읽힌다.**
검수 이력이 없는 `auditor3` 도 동일. 사장님(`owner2`)은 본인 53건만. 화면은 배정된 것만 보여 주지만
정책 `conversations_staff_read` 는 `current_role() in ('admin','auditor')` 로 전부 허용한다.

---

## 3. 데이터 설계

새 마이그레이션 `0037` ~ `0040`. 기존 `consultation_requests`(0034/0035)는 **경로 A 신청으로 그대로 유지**하고,
방·메시지·풀·제안은 새 테이블로 붙인다(기존 상태 기계를 건드리지 않는다).

### 3.1 `public.conversation_pool_consents` — 동의와 비식별 사본이 한 행에

| 컬럼 | 뜻 |
|---|---|
| `conversation_id` text PK → conversations(id) on delete cascade | 동의 단위 = 대화 1건 |
| `viewer_id` text | 동의한 사장님 domain_id |
| `granted_at` / `expires_at` bigint | 부여 시각 / +7일 |
| `revoked_at` bigint | 철회(= 즉시 풀에서 내려감) |
| `masked_payload` jsonb (nullable) | 마스킹된 Conversation — **풀에 보이는 전부**. **철회 시 null 로 비운다** |
| `mask_report` jsonb | `{규칙: 건수}` — 무엇을 몇 개 지웠는지(**messages 안에서만** 센다, §3.2) |
| `masked_at` bigint | 마스킹 시각(= 얼린 시점) |
| `occupation` / `tax_category` / `title` | 목록 메타. 원문 테이블을 조인하지 않으려고 동의 시점에 함께 얼린다(`title` 은 마스킹 후) |

- RLS: 사장님 = 본인 행 select 만, admin = 전체. **세무사는 정책이 없어 직접 조회 0행** — RPC 로만 본다.
  insert/update 정책 없음 — 쓰기는 `grant_pool_consent`/`revoke_pool_consent` 로만.

- **풀 노출 조건** = `revoked_at is null and expires_at > now`. 별도 플래그를 두지 않는다(어긋날 자리를 없앤다).
- 원문(`conversations.payload`)과 **분리 저장**한다. 풀은 이 테이블만 읽는다 → 마스킹 누락이 있어도
  "원문이 통째로 새는" 사고는 구조적으로 안 난다.
- 동의는 그 시점의 대화를 얼린다. 동의 후 이어진 턴은 풀에 안 올라간다(재동의로만 갱신).

### 3.2 마스킹은 **DB 함수 안에서** (`mask_conversation_payload(jsonb) returns jsonb`)

동의·마스킹·등재를 한 트랜잭션에 둔다. 클라이언트가 마스킹을 건너뛰고 등재할 경로 자체를 없애기 위해서다
(0034 에서 연락처 공개 범위를 `list_experts()` 로 강제한 것과 같은 판단).

| 규칙 | 패턴(요지) | 치환 |
|---|---|---|
| 전화 | `01[016-9]`+3~4자리+4자리, 지역번호형 | `[전화]` |
| 이메일 | RFC 근사 | `[이메일]` |
| 사업자등록번호 | 3-2-5 | `[사업자번호]` |
| 주민등록번호 | 6-7(성별자리 1~4) | `[주민번호]` |
| 계좌번호 | 2~6-2~6-2~8 (은행명 근접 시) | `[계좌]` |
| 카드번호 | 4-4-4-4 | `[카드]` |
| 주소 | 시/도 + 시·군·구 + 로/길 + 번지 근사 | `[주소]` |
| 상호·병원명 | `○○의원/병원/치과/한의원/약국/세무사무소` 접미 | `[상호]` |
| 사람 이름 | ① 성 + **2자** + (띄움?) + `원장/대표/실장/사장/과장/부장/팀장/차장/이사/씨` ② 직함 + 띄움 + 성 + 2자 + `입니다/님/씨/께서…`·문장부호 | `[이름]` (이름만 바꾸고 직함은 남김, 같은 이름은 전역 치환) |

**구현에서 정해진 것 (2026-09-21, 0037 실측)**
- **음성 장치**: 숫자 규칙은 앞뒤가 숫자·점이거나 `숫자,` 모양이면 안 잡는다(`1,000-2,000`·`0.010` 보호, 문장 속 쉼표 뒤 전화는 잡힘).
  계좌는 은행명·계좌·통장이 12자 안에 앞설 때만, 날짜(YYYY-MM-DD) 모양 제외. 상호는 후보를 찾은 뒤 일반명사 앞말(종합·대학·국회·치과·우리…)을 거른다.
  이름은 후보의 끝 글자가 조사면 명사+조사로 본다(띄운 경우 `이가는과와도만`, ②방향은 용언 어미 `해하한…`까지).
- **`persona.label`·`businessType` 은 규칙과 무관하게 `[고객]` 으로 바꾼다** — 계정 표시명("사장님2", 실가입자는 이름)이라 어느 계정의 사례인지 드러난다.
- 구조 키(`id`·`type`·`kind`·`role`·`occupation`·`taxCategory`…)는 건드리지 않는다(세그먼트 id 는 앵커, enum 은 스키마 검증).
- 리포트는 `messages` 안에서만 센다. 제목은 첫 질문의 사본이라 같이 세면 번호 하나가 "전화 3건"으로 부풀었다(E2E 발견).
- **알려진 한계(고지로 감당)**: 2자 이름("김철")·직함 없는 이름 미탐 / 대표번호(1588-…)는 연도 범위와 모양이 같아 미탐 /
  "인정은 원장" 류(명사+은) 과잉 마스킹 — 은은 이름 끝(지은·하은)에 흔해 빼면 실명이 새므로 감수. 실데이터 218건 기준 오탐 2건이 전부 이 유형.
- 단위시험: `supabase/tests/test_0037_pool_masking.py` (양성 22·음성 21 + json 재귀 + 역할별 RLS, 전부 롤백). 규칙을 고치면 여기에 사례를 더한다.

- **지울 수 없는 것이 남는다**: "강남에서 15년 된 치과", 금액대, 직원 수 같은 문맥 단서는 규칙으로 못 지운다.
  → 동의 시트와 풀 화면 양쪽에 **그대로 고지한다**("완전한 익명은 아닙니다"). 날조하지 않는 원칙과 같은 태도다.
- **동의 시트는 마스킹 결과를 먼저 보여 주고 동의를 받는다.** 안 보여 주고 받는 동의는 동의가 아니다.
- 규칙은 단위시험으로 양성·음성 둘 다 건다(전화번호처럼 생긴 금액 `1,000-2,000` 을 지우면 안 된다).

### 3.3 `list_pool_cases()` / `open_pool_case(id)` — 풀 조회 RPC (security definer)

세무사·admin 만 실행. 유효 동의만. **원문 테이블을 조인하지 않는다.**

- `list_pool_cases()` — **본문 없이** 요약 메타만: 업종·세목·마스킹된 제목·첫 질문·질문 수·등재/만료 시각·리포트·`viewed_by_me`.
  "내가 이미 제안했는지"는 제안 테이블이 생기는 **(c) 0039 에서** 열을 더한다(반환 열이 바뀌므로 `drop function` 후 재생성).
- `open_pool_case(id)` — 마스킹 사본 전문 + **열람 기록** 1건. 상세는 이 함수로만 열린다.
- `public.pool_case_views (conversation_id, auditor_id, first/last_viewed_at, view_count)` — §10-1 결정(기록만, 고객엔 비공개).
  쓰기는 `open_pool_case()` 안에서만, 읽기는 admin 만. 대화 삭제 시 cascade.
- 사장님 쪽: `preview_pool_mask(id)` — 본인 대화의 지금 payload 를 같은 함수로 마스킹해 보여 준다(저장 안 함).

### 3.4 `public.consultation_offers` — 경로 B: 세무사 → 고객 연결 요청

`id, conversation_id, expert_id, viewer_id, message(≤300자, 옵셔널 1건), status, status_history, created_at, updated_at, expires_at`
· `status`: `pending | approved | declined | withdrawn | expired` · `unique (conversation_id, expert_id)`

- 기존 `consultation_requests` 를 재사용하지 않는 이유: **방향과 승인 주체가 반대**다(세무사가 걸고 고객이 승인).
  같은 테이블에 방향 컬럼을 붙이면 0035 의 전이 함수가 두 갈래로 갈라진다.
- insert 는 `make_offer()` RPC 로만(세무사 본인 + 유효 동의 존재 + 상한 검사). 전이는 `transition_offer()` 하나로.
- 상한: **대화당 pending 제안 5건, 열린 방 3개.** 고객이 요청 더미에 깔리지 않게.

### 3.5 `public.consultation_rooms` — 채팅방

`id, conversation_id, viewer_id, expert_id, origin('request'|'offer'), origin_id, status('open'|'closed'),`
`created_at, closed_at, last_message_at, viewer_last_read_at, expert_last_read_at` · `unique (conversation_id, expert_id)`

방 개설 경로는 **`open_room()` RPC 하나**로 모은다. 경로 A 수락(`transition_consultation` 의 `accepted`)과
경로 B 승인(`transition_offer` 의 `approved`)이 각각 이 함수를 호출한다.

### 3.6 `public.consultation_messages` — 메시지

`id, room_id → rooms(id) on delete cascade, sender_id, sender_role('user'|'auditor'), body(1~4000자), created_at, edited_at, deleted_at`
· 인덱스 `(room_id, created_at)`

- RLS: **방 참여자(viewer/expert) + admin** 만 select/insert. update 는 본인 메시지의 `deleted_at` 만(soft delete).
- Realtime publication 에 `consultation_rooms`·`consultation_messages` 추가.
- **읽음**은 메시지별이 아니라 방의 `*_last_read_at` 한 칸으로. 메시지마다 읽음 행을 쌓지 않는다.

### 3.7 `conversations_staff_read` 축소 (D2)

교체 후 세무사는 다음 중 하나에 해당하는 대화만 읽는다.

1. 내가 픽업한 검수 건 — `audits.auditor_id = me`
2. 열린 일감에 실린 대화 — `audit_tasks.status in ('open','full','in_progress') and id = any(conversation_ids)`
3. 나와 방이 있는 대화 — `consultation_rooms.expert_id = me`
4. 내 앞으로 온 경로 A 신청 — `consultation_requests.expert_id = me`

admin 은 기존대로 전체.

②가 필요한 이유: `/audit/queue` 의 일감 상세가 **픽업 전에** 실린 대화를 미리 보여 준다(`queue-detail-view.tsx`).
이걸 빼면 큐 화면이 빈다.

**영향 화면 회귀 목록** (이 축소는 반드시 아래를 다시 돌리고 배포):
`/audit/queue`(목록·상세) · `/audit/work`(+워크스페이스) · `/audit/results`(목록·상세) ·
`/audit/consultations`(대화 원문 링크) · `/audit/kb-map` · admin 쪽 `pool-table`·`inspection-*`·`task-create-form`.
프런트는 `conversation-store` 가 **전체 fetch** 라 세무사 쪽 records 가 줄어들 뿐이지만, 위 화면들이
"목록에 있는데 대화가 없다"를 어떻게 그리는지 확인해야 한다(빈 제목·null 접근).

### 3.8 알림

- mail kind `consultation` **재사용**(0035). `ref` 에 `{kind:'offer'|'room', id}` 를 더한다
  → `Record<MailKind, …>` 라벨 맵을 다시 건드리지 않아도 된다(원본 가이드 §8.3 함정 회피).
- 새 제안 도착 → 고객 / 승인·거절 → 세무사 / 새 메시지 → 상대방.
  **메시지마다 메일은 금지** — 방당 1건(그 뒤로는 뱃지만).

---

## 4. 화면

### 4.1 사장님

- **사이드바 "상담 세션" 을 탭 2개로**: `AI 상담` / `세무사 채팅`
  - AI 상담 = 지금의 `liveSessions`(conversations)
  - 세무사 채팅 = `consultation_rooms`(상대 세무사 이름·아바타·마지막 메시지·안 읽음 수)
  - 모바일: 컨벤션대로 탭(`mobileShow`) 재사용
- **설정 메뉴(= 사이드바 하단 `AccountSwitcher` 드롭다운, 지금은 로그아웃뿐)** 에
  `세무사 연결 요청 N건` 추가 → `/offers`
  - 요청 목록: 세무사 프로필 카드(0034 `ExpertCardView` 재사용) + 동봉 메시지 1건 + 어떤 대화 건인지
  - `[승인]` → `open_room()` → 그 방으로 이동 / `[거절]`(사유 선택) / 7일 경과 자동 만료
- **동의 시트**: 경로 A 신청 시트에 체크 한 줄 + "무엇이 가려지는지 보기"(마스킹 미리보기 + `mask_report`)
- `/consultations`(기존 추적 화면): 연락처 자리 옆에 `[채팅방 열기]`, 동의 상태·만료일·`[동의 철회]`
- 채팅방 `/rooms/[roomId]`: 메시지 목록 + 입력창, 상단에 세무사 프로필과 원 대화 링크

### 4.2 세무사

- `/audit/pool` — **비식별 사례 풀**. 카드 목록(업종·세목·요약 첫 줄·경과일), 상세에서 마스킹된 대화 전문.
  상단 고지: "개인정보는 규칙으로 가렸습니다. 완전한 익명은 아닙니다. 사례 밖 용도로 쓰지 마세요."
  `[연결 요청]` → 메시지 1건(선택) → `make_offer()`
- `/audit/rooms` — 내 채팅방 목록 + 방 화면(사장님 쪽과 같은 컴포넌트, 역할만 다름)
- `/audit/consultations`(기존) — 수락 시 방이 열리므로 `[채팅방 열기]` 추가
- 사이드바 뱃지: 내 방 안 읽음 수(+ 선택적으로 풀 새 사례 수)

### 4.3 관리자

- `/admin/consultations` 에 탭 추가: 제안(offers)·방(rooms) 집계, 신고 처리, **강제 종료·동의 강제 철회**(브레이크)

---

## 5. 상태 기계

```
consultation_offers:  pending ─승인→ approved ─┐
                        ├─거절→ declined        ├→ open_room()
                        ├─세무사 철회→ withdrawn │
                        └─7일→ expired          │
consultation_requests: pending ─수락→ accepted ─┘   (기존 0035 그대로)
consultation_rooms:    open ─(양쪽 누구나 | admin)→ closed
pool_consent:          granted ─7일→ expired | ─철회→ revoked
```

원칙(0035 계승): **전이는 함수 하나로만.** 테이블에 update 정책을 열지 않는다.

---

## 6. 하지 않는 것 (이번 범위 밖, 명시)

- 파일·이미지 첨부, 음성 — 텍스트만
- 타이핑 표시·온라인 표시·푸시 알림
- 방 안 대화의 RAG 적재·기여/정산 연동 — 운영 흐름 6단계와 충돌 소지가 있어 별도 설계
- 비로그인 채팅. **상담 채팅은 로그인 필수**(AI 챗의 무인증 정책과 다르다 — 여기엔 상대가 있다)
- LLM 기반 비식별·요약(D1 에서 배제)

---

## 7. 위험과 대비

| 위험 | 대비 |
|---|---|
| 규칙 마스킹이 문맥 단서를 못 지운다 | 고지 + 7일 만료 + 철회 + admin 강제 철회(`revoke_pool_consent` 가 admin 허용 — 화면은 §4.3 단계), 풀 열람 로그(`pool_case_views`, (a) 구현) |
| 고객이 제안 더미에 깔린다 | pending 5 / 열린 방 3 상한, 만료 7일 |
| 방이 여럿이라 고객이 같은 말을 반복 | 방 개설 시 세무사에게 **원 대화 링크** 제공(승인된 방이므로 §3.7 ③으로 열람 가능) |
| 메시지 Realtime 유실 | (c)에서 고친 `makeCollectionSync` 순서 규칙(구독 → fetch → 버퍼)을 그대로 따른다. 방 화면은 **열 때 한 번 더 fetch** |
| `staff_read` 축소가 기존 화면을 깬다 | §3.7 회귀 목록 전수 + 별도 단계(d)로 분리해 롤백 가능하게 |
| 국내 AI 트랙 | 마스킹·매칭 전부 규칙 기반 — LLM 무관. 제품 구동 경로에 외산 모델 없음 |

---

## 8. 구현 단계 (새 세션, 단계마다 배포·검증)

| 단계 | 범위 | 끝나는 조건 |
|---|---|---|
| **(a)** | `0037` 동의·마스킹(`mask_conversation_payload`, `grant_pool_consent`, `revoke_pool_consent`, `list_pool_cases`) + 동의 시트·미리보기 + `/audit/pool` **읽기 전용** | 마스킹 규칙 단위시험(양성·음성), 역할별 RLS 검증, 사장님 동의 → 풀에 뜸 → 철회 → 사라짐 |
| **(b)** | `0038` 방·메시지(`open_room`, RLS, Realtime) + 방 화면(양쪽 공용) + 경로 A 수락 시 방 개설(D3) | 두 브라우저로 주고받기, 안 읽음 뱃지, 새로고침·재진입 |
| **(c)** | `0039` 제안(`make_offer`, `transition_offer`) + 세무사 `[연결 요청]` + 사장님 설정 메뉴 뱃지·`/offers` 승인·거절 | 경로 B 종단, 상한·만료, 메일 |
| **(d)** | `0040` `conversations_staff_read` 축소 + §3.7 회귀 전수 | 세무사 토큰 직접 질의에서 **배정 밖 대화가 안 보임**을 실측(오늘의 218건 시험을 반대로) |
| **(e)** | 사장님 사이드바 탭 분리(AI 상담 / 세무사 채팅) + 모바일 | 412px 가로 스크롤 0 |

(b)와 (c)는 순서를 바꾸지 않는다 — 방이 없으면 승인 버튼이 갈 곳이 없다.

---

## 9. 작업 환경 메모 (이 기기 실측, (c)까지 유효)

- 마이그레이션: `backend/.venv/Scripts/python.exe supabase/apply_migration.py <sql> <버전> <이름>` (끝의 검증 목록은 0007 전용 — 무시)
- RLS 검증: psycopg 트랜잭션 안에서 `set local role authenticated` + `set_config('request.jwt.claims', '{"sub":"<uuid>"}', true)` → 롤백.
  데모 uuid: viewer `1111…`, auditor `2222…`, admin `3333…`, auditor2 `4444…`, owner2 `5555…`, auditor3 `8888…`
  로그인: `<domain_id>@demo.local` / `demo1234` (사장님은 domain_id 가 `viewer` 인데 이메일·로그인 아이디는 `owner`)
- 로컬 검증: `next dev`(Turbopack)는 이 기기에서 CSS 워커 실패(0xc0000142) → `npx next build && npx next start -p 3012`.
  백엔드는 8787 점유 중이라 8788. Playwright 는 `import { chromium } from "file:///C:/Users/user/Neo-Luddite/frontend/node_modules/playwright/index.mjs"`
  (NODE_PATH 는 ESM 에 안 먹는다)
- **함정 모음**: `lib/sidebar-badges.ts` 는 UTF-16(`file` 이 data 로 본다) — 편집 시 인코딩 유지.
  Storage 직접 SQL DELETE 는 막혀 있다(Storage API 로만). Git Bash `curl` 로 한글 본문 → 인코딩 깨짐(python urllib 사용).
  긴 heredoc 으로 문서를 쓰면 셸이 삼킬 수 있다 — 문서 파일은 Write 로.
- 배포: 프론트 `git push origin HEAD:main`(Vercel), 백엔드는 `import-credigraph` push + `deploy.sh`.
  이번 설계는 **백엔드 변경이 거의 없다**(트리거는 이미 (a)에 있고, 마스킹은 DB).

---

## 10. 착수 전에 사용자에게 확인할 것

1. ~~풀에서 사례를 열람한 세무사를 **기록·공개**할까?~~ → **결정(2026-09-21): 기록만, 고객엔 비공개.** `pool_case_views` admin 전용.
2. 방 종료 권한: 양쪽 누구나 vs 고객만. 지금 설계는 **양쪽 + admin**.
3. 거절당한 세무사가 **같은 대화에 다시** 제안할 수 있는가. 지금 설계는 `unique(conversation_id, expert_id)` 로 **1회만**.
4. 상한 수치(pending 5 / 방 3 / 7일)가 전시 데모에 맞는지.

## 11. 진행 기록

- 2026-09-21: 설계 확정(D1~D4). 구현은 새 세션 (a)부터.
  `conversations_staff_read` 전면 허용을 이 날 실측으로 확인(세무사 토큰 → 전체 218건 payload 열람).
- 2026-09-21: **(a) 완료·배포.** 마이그레이션 `0037_pool_consents_masking.sql` 적용(운영 Supabase 도쿄), 프론트 main push.
  - 착수 전 결정: §10-1 열람 기록 = **기록만, 고객엔 비공개** → 0037 에 `pool_case_views` 추가.
  - DB: `conversation_pool_consents`·`pool_case_views` + `mask_conversation_payload`(내부 `_pool_mask_text`/`_pool_mask_jsonb`/`_pool_report_add`)
    + `preview_pool_mask`·`grant_pool_consent`·`revoke_pool_consent`(본인·admin)·`list_pool_cases`·`open_pool_case`.
    마스킹 함수는 사용자 실행 불가(revoke), 사용자 경로는 RPC 뿐.
  - 설계에서 바뀐 것(본문 §3.1~3.3 반영): 철회 시 사본 비움 / 목록 메타 컬럼 3개 / 목록·상세 RPC 분리(상세만 열람 기록) /
    `persona` 계정 표시명 `[고객]` 중화 / 리포트는 messages 만 / 이름 규칙 2방향·성+2자 / "내가 제안했는지"는 (c)로.
  - 프론트: `services/case-pool.ts`, `components/case-pool/{pool-parts,owner-pool-consent,expert-pool-view}.tsx`,
    연결 카드 시트에 체크(기본 꺼짐)+"무엇이 가려지는지 보기"(미리보기·리포트 칩·완전 익명 아님 고지), 동의 실패해도 신청은 유지,
    `/consultations` 상세에 "상담사 풀 공개"(상태·만료일·[동의 철회]), `/audit/pool[/id]` + 사이드바 "상담사 풀". 연결 요청 버튼은 (c).
  - 검증: DB 87/87(`supabase/tests/test_0037_pool_masking.py`, 적용 전 드라이런·적용 후·깨끗한 DB 재현 3모드),
    실데이터 218건 오탐 감사, 브라우저 E2E 21/21 **2회 연속**(사장님 412px 동의 → 다른 세무사 풀에 뜸·PII 없음·열람함 → 철회 → 사라짐, page error 0), tsc 0·lint 0·build ✓.
  - **E2E·감사가 잡아 고친 것**: ① 실데이터 "공간이 원장"·"명의가 원장님" 이름 오탐 → 띄운 경우 조사 끝 글자 제외
    ② "원장 김철수입니다"(직함 앞) 미탐 → ②방향 규칙 ③ 그 규칙이 "세무사 연결해 주세요"를 [이름]으로 → 직함에서 세무사 제외·뒤 조건 좁힘·용언 어미 제외
    ④ "전화 3건" 부풀림 → messages 만 셈 ⑤ 풀 카드에 "사장님2"(계정 표시명) 노출 → persona 중화·목록 열 제거
    ⑥ 끝 쉼표 가드가 "02-555-1234," 를 놓침 → `숫자,` 모양만 제외. ②~⑤는 적용 뒤 발견이라 함수만 `create or replace`(사용자 확인 3회), 파일=DB 일치 확인.
  - E2E 데이터(대화 5·신청 5·메일 5·chat_turns 5, 동의·열람은 cascade) 삭제 — 대화 218건 원복.
  - 남은 것: admin 강제 철회·열람 기록 **화면**(§4.3, 함수는 있음) / 만료된 사본 정리(pg_cron 미구현 — 목록에서만 빠지고 행은 남음) /
    풀 새 사례 뱃지(§4.2 선택) / 사장님이 신청 없이 동의만 하는 경로 없음(설계대로 경로 A 시트에서만).
