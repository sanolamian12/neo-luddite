# 다음 세션 프롬프트 — 인앱 상담사 채팅 후속 공사 3단계: 마감 지난 일감 닫기(#3)

아래를 새 세션 첫 메시지로 붙여 넣는다.

---

인앱 상담사 채팅 **후속 공사 3단계**(설계 §12 의 #3 마감 지난 일감 닫기)를 진행하자.
2단계(#1 admin 상담 운영 탭)는 2026-09-23 에 배포됐다(프론트 858dc11, Vercel success, **DB 변경 없음**).
남은 순서: **3단계 #3(이번) → 4단계 #2 채널 묶기.**

이 단계는 **코드보다 운영 결정이 먼저다.** 아래 "물을 것"을 먼저 묻고, 답이 나온 뒤에 범위를 짜라.

## 먼저 읽을 것
1. `design/인앱상담사채팅_상담사풀_설계.md` — **§12 남은 일·후속 순서·후속 공사 진행 기록(1·2단계 줄)**,
   **§3.7(0040 `conversations_staff_read` 축소 — 조건 ② 열린 일감)**, §7 위험표, §9 환경.
2. 왜 이 일이 남았나: 0040 으로 세무사의 대화 열람을 좁혔지만 **실측 218 → 206건에 그쳤다.**
   마감이 7월에 지났는데도 `full` 로 남은 **100건짜리 일감 2개**가 조건 ②(열린 일감에 실린 대화)로 대부분을 연다.
   일감이 닫히면 ②가 빠지고 축소 효과가 커진다 — 즉 이 단계는 **화면 기능이 아니라 0040 의 마무리**다.
3. 코드 자리(이미 있는 것):
   - `services/audit-task.ts` `forceClose(taskId)` — admin 이 일감을 `closed` 로. **개별 종료는 이미 된다**(`components/admin/task-detail-view.tsx`).
   - 상태는 `open | full | in_progress | closed`(`poc-schema.ts`), 라벨은 `poc-format.ts` `TASK_STATUS_LABEL`.
   - 0040 조건 ②는 `can_staff_read_conversation()` 안에서 `status in ('open','full','in_progress')` 를 본다 — `closed` 는 안 연다.

## 2026-09-23 실측 (일감 6개 전부 마감 경과)

| 일감 | 상태 | 마감 | 실린 대화 |
|---|---|---|---|
| `task-mrf000v5-ybc4` | full | 2026-07-19 | 100 |
| `task-mrkngt66-r7cy` | in_progress | 2026-07-22 | 1 |
| `task-mrlt1xl8-n4yb` | full | 2026-07-22 | 100 |
| `task-mruoa15c-ue9f` | in_progress | 2026-07-28 | 2 |
| `task-msflx0ac-tjdx` | in_progress | 2026-08-12 | 2 |
| `task-msgus7ik-da8e` | in_progress | 2026-08-13 | 1 |

검수 건(`audits`): draft **255** · cancelled 141 · reviewed 2 · finalized 159.
**draft 255 건이 이 일감들에 매달려 있다** — 일감을 닫을 때 이 초안을 어떻게 할지가 이번 결정의 핵심이다.

## 착수 전에 사용자에게 물을 것 (코드보다 먼저)
- **작성 중 초안(draft 255)을 어떻게 하나**: 그대로 두고 일감만 닫는다(초안은 남지만 새 픽업이 안 된다) /
  일감을 닫으면 그 일감의 draft 를 `cancelled` 로 함께 정리한다 / 초안이 있는 일감은 닫지 않는다.
- **일회성인가 자동인가**: 이번 6개만 손으로 닫는다 / admin 화면에 "마감 지난 일감 일괄 닫기" 버튼을 둔다 /
  pg_cron 으로 마감 지나면 자동으로 닫는다(**이 저장소에 pg_cron 은 이미 하차장 스냅샷에 쓰고 있다**).
- **닫는 기준**: 마감일 경과만 / 마감 경과 + 진행 중 검수 0건 / 사용자가 고른 것만.
- **`full`·`in_progress` 를 다 닫나**: 전부 / `full`(정원 찼는데 손 안 댄 것)만 먼저.

## 이번에 손댈 자리 (결정 뒤 확정)
1. 닫기 실행(일회성 SQL 이든 화면 버튼이든 pg_cron 이든 — 위 결정대로).
2. 닫은 뒤 **0040 효과 재실측**: 세무사 토큰 PostgREST `conversations` 건수(지금 206 → 몇으로 떨어지는지). §3.7 본문의 "218 → 206" 문장을 실제 값으로 고친다.
3. 회귀 기준 갱신 — **닫으면 기준값이 바뀐다**:
   - `regress.mjs` 가 `task-msgus7ik-da8e`(일감 상세)·`/audit/queue` 를 도는데 그 화면의 본문이 바뀐다 → 전·후 비교 기준을 새로 잡는다.
   - `supabase/tests/test_0040_staff_read.py --applied` 는 **보이는 건수 = 조건 정의대로 센 값**을 검사하므로 통과해야 정상이지만,
     실측 건수를 적어 둔 자리가 있으면 같이 고친다.
   - `e2e_staff_read.mjs`(32항목)는 E2E 일감을 직접 만들고 지우므로 영향이 없을 것 — 돌려서 확인한다.

## 하지 않을 것
채널 묶기(#2 — 4단계), admin 화면 추가 공사, 만료 행 정리(#6), 메시지 페이지네이션(#7), 풀 새 사례 뱃지(#8),
동의만 하는 경로(#9), 방 안 대화 RAG·첨부(#10).

## 검증 (이 프로젝트 관행)
- 닫기 전·후 **세무사 3명 토큰 실측**(`conversations` 건수, payload 포함) + admin·사장님·anon 대조.
- `regress.mjs LIVE=1` 52화면(admin 상담 운영 탭 4개 포함) — 일감 관련 화면의 본문 차이는 **의도한 것만** 남는지 눈으로 확인.
- (a) 21/21 · (b) 30/30 · (c) `e2e_offers.mjs` 44/44 · (d) `e2e_staff_read.mjs` 32/32 · (e) `e2e_tabs.mjs` 41/41 ·
  후속2 `e2e_admin.mjs` 40/40 · `e2e/sync-retry.spec.ts` 2/2.
- DB `test_0040 --applied` 52/52 · `test_0039 --applied` 76/76 · `test_0038 --applied` 77/77 · `test_0037 --applied` 87/87
  (**0037~0039 도 `--applied`** — 인자 없으면 CREATE 중복으로 멈춘다).
- tsc 0 · 새 파일 lint 0(전체 41 — 늘지 않으면 됨) · build ✓.
- E2E 데이터는 끝나고 지운다 — 대화 **218** / 신청 0 / 메일 1 / chat_turns 408 / 검수 557 / 방·메시지·제안·동의·열람 0 원복.
  **일감 6 은 이번 단계에서 바뀔 수 있다** — 바뀐 기준값을 `e2e_db.py` 출력과 함께 기록한다.

## 세션을 끝낼 때 반드시
1. 설계 마스터 §12 "후속 공사 진행 기록"에 **3단계 기록**(결정, 실행 방법, 0040 재실측값, 회귀 기준 변화)을 덧붙이고 #3 행을 완료로.
   **§3.7 의 "218 → 206" 문장과 "오래된 일감을 닫는 운영 절차가 생기면 효과가 커진다"를 실제 결과로 고친다.**
2. **다음 단계(4단계 #2 채널 묶기) 지시서** `design/다음세션_프롬프트_상담사채팅후속4.md` 를 쓰고 이 지시서는 지운다. `design/README.md` 목록 갱신.
   4단계는 §7 함정 넷이 다 걸리는 `sync.ts` 뼈대 변경이라 **회귀 범위가 가장 넓다** — `timing.mjs` 전·후(특히 admin 첫 화면 3.6~4.7초)가 판정 기준이라고 적어 둔다.
3. `history/` 세션 정리 md — `design/` 갱신분과 **같은 커밋**.
4. 메모리 `project_inapp_consultant_chat` 의 후속 공사 줄 갱신.

## 환경·배포
설계 §9 그대로. 2단계에서 확인한 것:
- 로컬 E2E 빌드: `NEXT_PUBLIC_API_BASE=http://localhost:8788 NEXT_PUBLIC_CHAT_MODE=remote npx next build && npx next start -p 3012`,
  백엔드 `CORS_ORIGINS=http://localhost:3012 backend/.venv/Scripts/python.exe -m uvicorn api.main:app --port 8788`(backend/ 에서).
  재빌드 전 3012 를 쥔 옛 `next start` 를 죽인다(`netstat -ano | grep :3012` → `taskkill //PID <pid> //F`).
- 기준(전) 측정이 필요하면 `git stash push -u` → 빌드·측정 → `git stash pop`(worktree 보다 간단하다, 2단계에서 이렇게 했다).
- 로그인: 사장님 `owner`(domain_id `viewer`), 세무사 `auditor`·`auditor2`·`auditor3`, `admin`, 전부 `demo1234`.
- 스크립트는 2단계 세션 scratchpad 에 있다:
  `C:\Users\user\AppData\Local\Temp\claude\c--Users-user-Neo-Luddite\0c0b075f-01eb-4ca2-ba57-26d1241f167f\scratchpad\`
  — `e2e_db.py mark|show|cleanup`(건수 목록에 `pool_case_views` 포함), `run_all.sh <스크립트> <회차>`,
  `e2e_admin.mjs`(후속2, 40항목)·`e2e_offers.mjs`(44)·`e2e_rooms.mjs`·`e2e_pool.mjs`·`e2e_staff_read.mjs`·`e2e_tabs.mjs`,
  `regress.mjs <라벨>`(`ONLY=`·`OWNER_ROOM=`·`LIVE=1`)·`diff_reg.py <전> <후>`(화면별 본문 길이 차이)·`regress_pre2.json`·`regress_post2.json`(2단계 전·후 52화면),
  `regress_room.py make|drop`, `timing.mjs`(`ONLY=admin` 등), `prod_smoke_admin.mjs`(운영 admin 탭), `e2e_sql.py task|close|insert_conv`, `tasks_probe.py`.
  **scratchpad 를 옮기면 스크립트 안 `OUT` 경로의 세션 id 를 새 것으로 바꾼다**(`sed -i 's/0c0b075f-01eb-4ca2-ba57-26d1241f167f/<새 id>/g' *.mjs *.py *.sh`).
- **Bash 인라인 python/node 에 `\n`·`\b` 금지** — 정규식이 든 수정은 Write 로 스크립트 파일을 쓴 뒤 실행(2단계에서 또 걸렸다).
- 감사 화면엔 `<main>` 이 둘 — Playwright `locator("main")` 은 strict 오류, `body` 를 쓴다.
- 모바일 사이드바 시트 셀렉터는 `[data-mobile="true"][data-sidebar="sidebar"]`.
- 간헐 잡음(이번 변경과 무관, 재현되면 기록만): 운영 `/audit/kb-map` 데스크톱 스피너가 20초 넘게 남는 일 · 로컬 `/admin/inspection/<id>` 412 가 한 번 "찾을 수 없습니다"로 뜬 일.
배포는 프론트 `git push origin HEAD:main`, 확인 `curl https://api.github.com/repos/sanolamian12/neo-luddite/commits/<sha>/status`.
**배포·마이그레이션 적용은 실행 전에 사용자 확인.** `design/` 은 추적 대상, `docs/` 는 로컬 전용이다.
