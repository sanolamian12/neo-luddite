✅ **단계 2·3 은 2026-09-25 에 끝났다** — `agentic-v2` `80a6093`. `?pipeline=v2` 가 §3 흐름(추출·닫힌 대조·D3·D4·버튼)을 탄다. 로컬 3턴 시나리오 + 버튼 통과.
남은 순서: **단계 4(데이터 세션) → 단계 5(제품 세션)**. 둘은 세션을 나눈다(메모리 `feedback_session_scope_separation`).

**먼저 읽는다**
1. `design/LLM1v2_역할축소_구현설계.md` — **§6 O-2 확정 모양 · §7-4~6(측정 밖 개작) · 부록 B** · 진행 기록 맨 아래
2. `history/260925_LLM1v2_단계2와3_…md` — 설계가 틀렸던 것 다섯 가지(특히 order 충돌·"모르겠어요"가 stated 로 삼켜짐)
3. 하니스 `docs/doing/260924_LLM1충분성평가/` · W2 궤적 `…/derived/trajectories_w2.jsonl`(63건, 턴별 `filled`/`unknown`)

═══ 단계 4 — 데이터 세션: 턴 단위 평가 ═══
- 대상 = **제품 원문**: `backend/api/prompts/v2/check_system.txt` + `llm1.check_asked()`(입력 = 역할 붙인 대화 + "앞서 물은 사실"). 하니스 부록 B 가 아니다.
- W2 궤적 2턴의 물은 사실 → `stated/unknown/missing` 을 궤적 라벨(`filled`/`unknown`)과 대조. 기대치 = 오라클 v2(기재→stated 73%, 미기재→missing 91%) 근처.
  **`unknown` 재현율을 따로** 센다(새 값, 스모크에서 "…너무 오래돼서 모르겠어요" 실패). 부정형 답 → stated 도 따로(스모크 "운행기록부 안 써요"가 missing).
- 부수 계측: `extract_facts` 의 `droppedNumeric` 빈도(결정적 사실을 잃나), `write_questions` 폴백 빈도.
- 프롬프트를 고치면 **제품 파일을 고친다**(agentic-v2) — 하니스 사본과 갈라지지 않게. 채점에 Claude 는 오프라인만(트랙 무관).

═══ 단계 5 — 제품 세션: v2 유닛 기동 + 소수 트래픽 ═══
- 서버 `/opt/neo-luddite-v2` 에서 `git pull`(agentic-v2) → **`BACKGROUND_JOBS=off` 확인** → `systemctl enable --now neo-luddite-api-v2`. 적용 전 사용자 확인.
- Vercel **Preview** env 에 `NEXT_PUBLIC_CHAT_PIPELINE=v2`(Production 금지). agentic-v2 프리뷰 URL 에서 E2E.
- 원복 리허설: 실질 킬 스위치 = v2 유닛 `disable --now`(Caddy 가 v1 로 흘린다, §4-1).
- 운영 지표 기록 자리 결정: `meta.llm1` 은 DB 에 안 남는다 — outcome(`v2_*`)만으로 되묻기 턴 수·gap 비율·버튼 사용률을 셀 수 있는지, 아니면 컬럼이 필요한지(**마이그레이션이면 사용자 확인**).

═══ 재논의 금지 ═══
- LLM1 은 1턴에서 충분을 판정하지 않는다 · D4 는 로직 · 2턴 대조는 닫힌다 · D3·D4 값 · action 3종 · O-1·O-2 · solar-pro3 · KB2 폐기.

═══ 하지 말 것 ═══
- `import-credigraph`·`main` 에 v2 코드 push · 프론트 `main` push · 유닛의 `BACKGROUND_JOBS=off` 제거.
- `pipeline.py`·`llm.py`·`handoff.py` 수정 · 기존 테이블 ALTER/DROP.
- 로컬 스모크에서 프로덕션 DB 접근 — 백엔드는 `SUPABASE_DB_URL=""`, 브라우저 스모크는 Supabase 호스트 abort(9/25 방식, 스크립트는 history 기록 §3).

═══ 알고 갈 것 ═══
- 직전 assistant 는 **목록 위치**로 찾는다(order 는 2턴부터 겹친다).
- 답변 단계는 LLM2 전까지 판정 없는 자문 임시(`write_advisory`) — 법령 조항을 지어낼 수 있고 1회 243초 멈춤을 봤다. 품질 개선은 W5 몫.

═══ 세션 마무리 ═══
1. 설계 마스터 § 진행 기록 + 어긋난 본문 수정. 2. `history/2609XX_…md`. 3. 메모리 `project_llm1_pivot`.
4. 다음 프롬프트를 `design/` 에 새로 쓰고 **이 파일은 지운다**. 5. 문서는 import-credigraph 에 커밋 → `agentic-v2` 로 merge(반대 방향 금지).
