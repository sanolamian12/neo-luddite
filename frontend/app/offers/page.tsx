import { OwnerOffersView } from "@/components/offer/owner-offers-view";

/** 세무사 연결 요청 — 풀에서 내 사례를 본 세무사가 보낸 요청(경로 B). 설정 메뉴에서 들어온다. */
export default function OwnerOffersPage() {
  return <OwnerOffersView />;
}
