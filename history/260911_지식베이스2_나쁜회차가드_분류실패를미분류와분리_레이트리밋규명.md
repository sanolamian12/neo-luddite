# 지식베이스2 나쁜 회차 가드 — "회차 붕괴"의 정체는 레이트 리밋이었다

- 날짜: 2026-09-11 (시드니 15:23 시작 / KST 14:23)
- 브랜치: `import-credigraph` · 커밋 `58a981f`
- 재구조화 job: `da0d2a7d-9a2a-44ec-a1c6-bb410e8f4724` (완주 664초)

---

## 0. 한 줄 요약

지난 세션이 "회차가 통째로 무너진다"고 본 현상은 **모델이 아니라 `RateLimitError`**
였다. `classify_dynamic_category` 가 `except Exception: return "미분류"` 라 **API 실패가
분류 판정으로 둔갑**해 있었다. 실패를 분리하고, 적재 직전에 멈추는 가드를 실측 기반
임계값으로 넣었다.

---

## 1. 먼저 잰 것 — 회차 편차

임계값을 감으로 정하지 않기 위해 편차부터 쟀다. 같은 150건·같은 목차(활성 30개)·
`temperature=0`, **제품 경로와 동일한 순차** 4회:

| 회차 | 배정률 | 호출 실패 |
|---|---|---|
| 1 | 52.0% | 0 |
| 2 | 53.3% | 0 |
| 3 | 53.3% | 0 |
| 4 | 51.3% | 0 |

**절대 2.0%p, 상대 3.8%.** 회차 편차는 사실상 없다. 호출 실패는 600건 중 0.

## 2. 그런데 지난 세션은 왜 40/38/75% 를 봤나

같은 측정을 **8워커 병렬**로 돌리자 그 붕괴가 재현됐다 — 그리고 원인이 로그에 찍혔다:

```
probe8 r1: 42/100 (42.0%) err=16 {'RateLimitError': 16}
```

413건 전량을 8워커로 돌린 판에서는 배정률이 **27.4% / 14.5%** 까지 내려갔다.
`except Exception: return "미분류"` 가 429 를 전부 '미분류'로 접었기 때문에, 호출측에는
"모델이 갈 곳 없다고 판단한 건"과 "물어보지도 못한 건"이 똑같이 보였다.

> **교훈**: 지난 세션 §4 의 "예외 0건" 관찰은 예외를 셀 수 없는 함수를 통해 얻은
> 것이었다. 폴백이 있는 함수로 무언가를 측정할 때는, **폴백이 삼킨 것부터 세야 한다.**
> 9/11 오전의 "같은 결과 = 그 단계가 둘 다 안 돌았다" 교훈과 같은 계열이다.

이로써 갈래가 하나 정리됐다: **미분류 ~44% 는 안정적이고 실재하는 값**이다(회차 편차가
아니다). 남은 커버리지 누수는 merge 품질 문제로 확정된다.

---

## 3. 조치 (커밋 `58a981f`)

### ① 실패 ≠ 미분류 — `llm.classify_dynamic_category`

반환을 `(카테고리, 실패사유)` 튜플로. 사유는 `RateLimitError` 같은 예외명,
`no_tool_call`(tool_choice 로 강제했는데 안 지킴), `off_enum`(목차 밖 레이블).
모델이 진짜로 '미분류'를 고른 경우만 사유가 `None` 이다. 재시도도 1 → 2.

### ② 나쁜 회차 가드 — `kb2_taxonomy._assess_run`

`archive_all_active_documents()` **직전**에서 호출한다. 거기가 되돌릴 수 있는 마지막
지점이다 — 카테고리 생성도 archive 뒤라, 중단 시점엔 아무것도 안 건드린 상태다.

중단 조건 두 개:

| 조건 | 값 | 근거 |
|---|---|---|
| 호출 실패율 | > 2% | 순차 실측 0/600건이라 평시값이 0. 실제 사태는 16% 로 자릿수가 다르다 |
| 배정률 | < `max(35%, 직전세대×0.75)` | 상대 25% 하락 = 관측 편차(3.8%)의 6배 |

기준선은 신설한 `kb2_store.get_latest_generation_job()` — `status='done'` **이면서
coverage 가 있는** job 만 본다. 기존 `get_latest_finished_job` 은 error 를 포함해서,
**가드가 한 번 중단시키면 그 error job 이 기준선이 돼 가드가 스스로를 무력화**한다.

설계를 정한 비대칭: 좋은 회차를 한 번 놓치는 비용은 "내일 밤 다시 돌린다"지만, 나쁜
회차를 한 번 통과시키는 비용은 "세대가 사라졌다"다. 그래서 애매하면 중단 쪽으로 기운다.

중단된 회차는 `stage='aborted'` + 계측을 함께 남긴다. 스케줄러는 `status='error'` 를
재시도하지 않는다(새벽에 조용히 API 예산을 태우지 않는다).

### ③ 화면 — "실패"가 아니라 "중단됨"

가드가 멈춘 회차는 빨간 "실패"가 아니라 호박색 **"중단됨 — 기존 세대를 지켰습니다"**
로, 이번/직전/요구 배정률과 실패 사유를 함께 보여준다. 제일 중요한 한 줄은 "기존 세대는
그대로다"이다 — 재구조화 실패 화면을 보면 KB 가 비었을까 봐 놀라는데 가드는 정확히 그
반대를 한 것이다. 통과한 회차도 실패 건수가 0 이 아니면 노출한다.

### ④ 맵 단계 `maxItems` (로드맵 §7 "다음 세션 후보" 3번)

맵 도구 스키마에 `minItems 3 / maxItems 6`. 산문으로 "3~6개"라고만 말해 배치당 18~32개를
내던 것을, 리듀스에서 통한 처방과 같게 맞췄다.

### 가드 검증 — 실제 기준선(57.9%)에 대고 8개 시나리오, 읽기 전용

| 시나리오 | 판정 |
|---|---|
| 51.3 / 53.3 / 57.9% (실측 정상) | 통과 |
| 27.4% / 14.5% / 0% (실측 붕괴) | **중단** |
| 배정률 정상 + 429 16% | **중단** |
| 배정률 정상 + 429 1건 | 통과 |

---

## 4. 배포 + 재구조화 실측

`58a981f` → `import-credigraph` push → `deploy.sh` → health
`{"ok":true,"model":"solar-pro3"}` / `{"ragEnabled":true,"kbPassages":413}` → `main` push.

재구조화 job `da0d2a7d` 완주 664초:

```
전(9/11 오후): 413 → 배정 239 (57.9%) → 투입 239 (절단 0) → 인용 223 (54.0%) / 문장 379 / 1013초
후(이번):      413 → 배정 231 (55.9%) → 투입 231 (절단 0) → 인용 215 (52.1%) / 문장 365 /  664초
```

### 읽는 법 — 커버리지는 **개선되지 않았다**

- 커버리지 54.0% → 52.1%(−1.9%p), 배정 57.9% → 55.9%. 상대 3.5% 하락으로 **측정된 회차
  편차(3.8%) 안**이다. 즉 **좋아지지도 나빠지지도 않았다** — `maxItems` 는 미분류를
  건드리지 못했다. (그리고 §1 의 규칙대로, 1회 측정으로 "나빠졌다"고도 말할 수 없다.)
- **가드가 정상 회차를 막지 않았다**(55.9% vs 요구 43.4%). 오탐 없음이 확인됐다.
- **분류 호출 실패 0건** — 프로덕션 순차 경로에서 §1 의 예측(0/600)이 그대로 재현됐다.
  이제 이 값이 계측에 남아, 다음에 429 가 나면 '미분류'에 섞이지 않고 바로 보인다.
- 지난 세션 성과는 유지: 합성 인용률 **93.1%**(215/231), 환각 id **0**.
- 완주 1013초 → **664초**(−34%). 맵이 배치당 후보를 ≤6개만 내니 리듀스 입력이 줄었다.

### 남은 문제는 선명해졌다 — merge 가 **근사 동의어**를 못 접는다

배정 0건 세목이 4개 → **6개**로 늘었고, 겹치는 레이블이 그대로 살아남았다:

- 복리후생 계열 **4개**: `인건비·복리후생`(26) / `복리후생비`(18) / `복리후생비처리`(10)
  / `피부시술복리후생비`(6)
- `접대비`(14) / `접대비인정`(3)
- `업무용승용차특례`(14) / `업무용승용차`(10)
- `인테리어비용처리`(12) / `인테리어·개인카드`(0) / `인테리어적격증빙`(0) / `적격증빙`(11)
- 질문 하나짜리 잔존: `6세이하자녀`(1), `육아휴직수당과세`(4), `1세대1주택비과세`(0),
  `수도권공공기관이전`(0)

merge 프롬프트는 "의미가 겹치거나 동의어인 카테고리는 하나로 합치라"고 이미 말하고
있다. **개수 제약(minItems/maxItems)은 잘 듣지만 의미 병합은 안 듣는다** — 30개를 채우라고
하면 근사 동의어를 쪼개서라도 30개를 만든다. 이게 지금 미분류의 실체다: 상담이 갈 곳이
없어서가 아니라, **갈 곳이 네 갈래로 쪼개져 있어서** 분류기가 고르지 못한다.

---

## 5. 날짜 정정 (파일명)

커밋 타임스탬프로 확인한 결과 `260912_지식베이스2_합성인용누수_…` 는 **오기**였다.
해당 세션 커밋(`9ae9b9d`~`6571a8d`)은 전부 **2026-09-11 13:55~14:33 (+1000)** 이다.
`260911_…` 로 `git mv` 하고, 코드·문서의 `2026-09-12` 참조를 전부 `2026-09-11` 로
정정했다. 하루에 파일 여러 개는 이미 컨벤션이다(`260909_` 3개, `260910_` 2개).

---

## 6. 다음 세션 작업 목록

### ① merge 하한 실험 — 개수와 중복을 **분리**해서 잰다 (최우선, 재구조화 불필요)

**가설**: `MERGE_MIN_CATEGORIES=20` 이 근사 동의어 분할을 *강제*한다. 30개를 채우라는
요구를 만족시키려고 모델이 `복리후생비` / `복리후생비처리` / `피부시술복리후생비` 로
쪼갠다면, 미분류의 원인은 "목차가 좁아서"가 아니라 "목차가 중복이라서"다.

⚠️ **이 가설은 9/11 오전 결론과 충돌한다** — 거기서는 목차 14개 47% vs 30개 28%로
**좁히면 더 나빠진다**고 나왔다. 그래서 "하한을 낮춘다"를 그냥 시도하면 안 되고,
**개수 축과 중복 축을 분리**해야 한다. 셋 다 같은 150건으로 분류해 배정률을 비교:

| 목차 | 개수 | 중복 | 무엇을 가리나 |
|---|---|---|---|
| A 현행 | 30 | 있음(복리후생 4갈래) | 기준선 |
| B 중복만 제거하고 **새 주제로 채움** | 30 | 없음 | 개수 고정 → **중복만의 효과** |
| C 중복만 제거 | ~22 | 없음 | 개수도 같이 줄었을 때 |

- B > A 이면 **중복이 범인** → 하한은 유지하고 의미 병합을 고친다.
- C < B 이면 9/11 결론(좁으면 나쁨)이 재확인 → **하한을 낮추면 안 된다.**
- A ≈ B ≈ C 면 목차 쪽이 아니라 분류 프롬프트 쪽을 봐야 한다.

**방법**: 재구조화 없이 된다. 맵 후보를 한 번 뽑아 파일로 캐시해두고(맵은 결정적이지
않으니 **캐시가 필수** — 안 그러면 목차 변형 간 비교에 맵 편차가 섞인다) merge 만 조건별로
다시 돌린다. 목차 3종이 나오면 §7 측정 절차로 각각 **순차 3회**.
소요: 목차 생성 ~5분 + 분류 9회 × ~110초 ≈ 25분.

**실패 시 대안(c)**: LLM 에 의미 병합을 맡기지 말고 **레이블 임베딩 코사인으로 사후
병합**한다. `tally_candidates` 가 "관찰 횟수는 파이썬이 공짜로 센다"로 성공한 것과 같은
수법 — 모델에게 못 시키는 판단은 코드가 한다.

### ② '기타' 세목 — 버리지 말고 보이게 (**사용자 결정 필요**)

지금 미분류 ~44%(182건)는 **어느 문서에도 안 실리고 사라진다**. ①이 성공해도 0 이 되지는
않는다. 트리에 옅게 남기는 기존 컨벤션(끊긴 세목)을 그대로 써서 세무사가 직접 분류할 수
있게 할지 결정 필요. 검색 포함 여부가 쟁점 — 잡다한 문장이 KB2 검색을 오염시킬 수 있어
**"트리에는 보이되 `match_sentences` 에서는 빠지는"** 형태가 유력.

### ③ 합성 단계 가드 (구멍 하나 남음)

가드는 분류까지만 본다. 분류를 통과해 **archive 한 뒤** 합성이 전량 실패하면 빈 세대가
남고 되돌릴 수 없다. 관측된 적은 없지만 구조적으로 열려 있다. 적재를 트랜잭션으로 묶거나,
합성 결과가 비면 archive 를 롤백하는 쪽.

### ④ 의미 중복 문장 dedup

지금 dedup 은 **정규화 일치만** 접는다. 청크가 잘아지며(41청크) 같은 조항이 다른 문장으로
두 번 들어올 수 있다. ①의 임베딩 병합과 같은 도구를 쓸 수 있다.

### ⑤ 잔가지

- `kb2.document_events` 이력 보기 UI — 지금은 마지막 사유만 배지로.
- 가드가 **실제로 중단시킨 적은 아직 없다**(프로덕션에선 통과만 봤다). 오프라인 8개
  시나리오로는 검증됨. 실전 발화를 보려면 임계값을 일시적으로 올려보는 정도.

---

## 7. 측정 방법 (재현용)

지난 세션 §7 에 더해, 이번에 덴 함정:

1. ⚠️ **폴백이 있는 함수로 측정하지 말 것.** `classify_dynamic_category` 처럼
   `except: return <기본값>` 이 있으면 실패가 데이터로 둔갑한다. 측정판은 예외를 따로
   세는 사본을 쓴다(`measure_classify.py` 의 `classify_instrumented`).
2. ⚠️ **속도 내려고 병렬로 돌리면 측정이 오염된다.** 8워커면 `RateLimitError` 가
   16% 나온다. 제품이 순차면 측정도 순차여야 한다 — 150건에 ~110초.
3. enum 순서도 조건이다 — 목차는 `order by created_at`(merge 순서)으로 꺼낼 것.
   알파벳 정렬은 제품 경로와 다르다.
4. `load_dotenv("backend/.env")` 명시 / `PYTHONIOENCODING=utf-8` + 결과를 파일로.
5. job 조회는 `GET /admin/kb2/restructure/{jobId}`, 응답이 `job` 키 아래 중첩.

---

## 8. 다음 세션 시작 프롬프트

```
docs/doing/RAG_구조분석_및_개선로드맵.md §7 을 읽어줘. KB2 로드맵 1~15번은 전부
완료·배포된 상태다. history/260911_지식베이스2_나쁜회차가드_분류실패를미분류와
분리_레이트리밋규명.md 에 직전 세션 경과가 있으니 §6(작업 목록)과 §7(측정 방법)을
특히 봐줘.

현재 상태 — 커버리지 52.1% (job da0d2a7d, 완주 664초):

    413 → 배정 231 (55.9%) → 투입 231 (절단 0) → 인용 215 (52.1%)
    문장 365 / 활성 문서 24 / 카테고리 30 / 분류 실패 0 / 환각 id 0

지난 세션에 "회차 단위 붕괴"의 정체를 밝혔다 — 모델이 아니라 RateLimitError 였고,
classify_dynamic_category 의 `except: return "미분류"` 가 API 실패를 분류 판정으로
둔갑시키고 있었다. 실패를 분리했고, 적재 직전에 멈추는 나쁜 회차 가드를 실측 기반
임계값으로 넣었다(순차 4회 배정률 52.0/53.3/53.3/51.3%, 실패 0/600 → 편차 상대 3.8%).
프로덕션에서 가드 통과 확인(오탐 0). **커버리지 자체는 안 움직였다.**

오늘 주제(우선순위순):

1. **merge 하한 실험 (최우선, 재구조화 불필요 ~25분)** — 미분류 ~44%의 실체는
   "갈 곳이 없어서"가 아니라 **"갈 곳이 여러 갈래로 쪼개져 있어서"**로 보인다:
   복리후생 계열이 4갈래(인건비·복리후생 26 / 복리후생비 18 / 복리후생비처리 10 /
   피부시술복리후생비 6), 접대비/접대비인정, 업무용승용차특례/업무용승용차,
   인테리어 3종+적격증빙. 배정 0건 세목도 6개다.
   가설은 MERGE_MIN_CATEGORIES=20 하한이 분할을 강제한다는 것.
   ⚠️ **단 이 가설은 9/11 오전 결론(목차 14개 47% vs 30개 28% — 좁히면 더 나쁘다)과
   충돌한다.** 그러니 하한을 그냥 낮추지 말고 §6①의 A/B/C 설계로 **개수 축과 중복
   축을 분리해서** 재줄 것. 맵 후보는 반드시 캐시해두고 merge 만 조건별로 다시 돌린다.

2. **'기타' 세목** — 미분류 182건이 지금은 어느 문서에도 안 실리고 사라진다.
   트리에 옅게 남기는 기존 컨벤션으로 세무사에게 보이게 할지 **내 결정 필요**.
   쟁점은 검색 포함 여부("트리에는 보이되 match_sentences 에선 빠지는" 형태가 유력).

3. 합성 단계 가드 — 분류는 막지만, archive 뒤 합성이 전량 실패하면 빈 세대가 남는다.

중요 — 이미 틀린 것으로 확인된 가설(다시 파지 말 것):
- **맵 프롬프트는 커버리지의 원인이 아니다** (9/11 오전 대조군).
- **맵 maxItems 도 아니었다** (9/11 저녁 — 완주는 34% 줄었지만 커버리지는 편차 안).
- **분류 배치화 금지.** 미분류 84% vs 건별 45%. llm.classify_passages_batch 는 경로에서
  분리돼 있고 docstring 에 경고가 있다.
- **"미분류 재시도는 무효 / 회차가 갈릴 뿐"은 폐기됐다** — 그 측정이 429 를 미분류로
  세고 있었다.

측정 방법 (위 문서 §7 에 상세):
- ⚠️ **폴백이 있는 함수로 측정하지 말 것.** `except: return <기본값>` 이 있으면 실패가
  데이터로 둔갑한다. 예외를 따로 세는 사본을 쓸 것.
- ⚠️ **속도 내려고 병렬로 돌리지 말 것.** 8워커면 RateLimitError 16%. 제품이 순차면
  측정도 순차(150건에 ~110초).
- 목차는 `order by created_at`(merge 순서)으로 꺼낼 것 — 알파벳 정렬은 제품과 다르다.
- 1회 측정으로 판단 금지. 최소 3회, 개별값을 같이 볼 것(편차 상대 3.8%).
- load_dotenv("backend/.env") 명시 / PYTHONIOENCODING=utf-8 + 결과를 파일로 받아 읽기.

주의사항:
- **이 세션은 제품 코드 전용.** 논문·벤치마크·채점은 다른 세션 사안이다
  (docs/doing/HF_사용정책_국내AI트랙.md §5). backend/api/ 런타임은 Upstage 전용 유지.
- 재구조화를 돌리면 활성 문서·카테고리가 전부 세대교체된다. 실행 전에 나한테 물어봐줘.
  **완주 약 11분**(664초). 컴퓨터는 꺼도 서버에서 계속 돈다.
- **재구조화가 도는 중에는 배포 금지** — deploy.sh 가 systemctl restart 다.
- 수정하면 순서대로: ①git push origin import-credigraph → ②backend/deploy/deploy.sh
  → ③프로덕션 curl 확인 → ④git push origin main(=Vercel 프론트). 배포는 매번 물어봐줘.
- **docs/ 는 git 미추적(로컬 전용)이다.** 커밋되는 건 history/ 뿐.
- 계측 화면 https://neo-luddite.vercel.app/admin/kb2 (admin/demo1234).
  job 상태는 GET /admin/kb2/restructure/{jobId}, **응답이 job 키 아래 중첩**이다.
- 세션 시작 시 먼저: git log --oneline origin/import-credigraph..import-credigraph
- 세션 끝나면 로드맵 §7 과 history/ 에 오늘 날짜로 기록. 날짜는 **나한테 확인**할 것
  (직전에 260912 로 하루 앞서 기록된 오기가 있었다).
```
