# 인앱 상담사 채팅 후속 3단계 — 마감 지난 일감 일괄 닫기(#3), 0040 의 마무리

2026-09-23 · 프론트만(main push) · **DB 마이그레이션 없음** · 설계 마스터 `design/인앱상담사채팅_상담사풀_설계.md` §12 3단계

## 한 줄

0040 으로 세무사 열람을 좁혔는데도 218 → 206건에 그쳤던 이유는 **마감이 7월에 지났는데도 열려 있던 일감 6개**였다.
admin 화면에 일괄 닫기 버튼을 만들어 6건을 닫자 **세무사 열람이 206 → 170 / 193 / 194건**으로 떨어졌다. 화면 기능이 아니라 0040 의 마무리다.

## 착수 전 결정 (사용자)

| 물은 것 | 답 | 이유 |
|---|---|---|
| 작성 중 초안(draft 255) | **그대로 둔다** — 일감만 닫는다 | 데이터 손실 0, 되돌리기 쉬움. 초안은 조건 ①(내가 픽업한 검수 건)로 계속 열린다 |
| 일회성인가 자동인가 | **admin 화면에 일괄 닫기 버튼** | 일회성 SQL 은 다음에 또 쌓이고, pg_cron 자동은 운영자가 못 막는다 |
| 닫는 기준 | **마감일 경과만** | 진행 중 검수가 있어도 지난 것은 지난 것 |
| 대상 상태 | **`full`·`in_progress` 전부** | 6개가 모두 마감 경과 |

## 지시서가 틀렸던 것

"개별 종료는 admin 화면에 이미 있다(`task-detail-view.tsx`)" — **사실이 아니었다.**
`services/audit-task.ts` 의 `forceClose(taskId)` 는 어느 화면에서도 불리지 않던 죽은 코드였다. 이번 버튼이 첫 UI 호출자다.

## 만든 것 (2파일, 40줄)

- `lib/poc-format.ts` — `isOverdueTask(task, now = Date.now())`. 렌더 중 `Date.now()` 를 직접 부르면 `react-hooks/purity` lint 오류가 난다
  (이 저장소의 Next 는 그 규칙을 켜 두고 있다) → `formatRemaining` 과 같은 **기본인자 꼴**로 뺐다.
- `components/admin/tasks-table.tsx` — 목록 헤더에 `ConfirmAction`(후속2 의 `admin-ops-parts`)으로 **"마감 지난 Task 닫기 (N)"**.
  해당 일감이 0건이면 버튼이 아예 없다. 경고 문구 → 확인 → `forceClose` 순차 호출.

## 실행 결과 (라이브)

- 일감 6건 전부 `closed`. `audits` draft **255 그대로**(그중 247건이 `task-mrlt1xl8-n4yb` 에 매달려 있었다).
- 닫힌 일감의 내 검수 건도 세무사 워크스페이스에서 **대화 원문이 그대로 열린다** — 조건 ① 이 살아 있다(스크린샷 확인).
- 세무사 큐는 "픽업 가능한 작업이 없습니다", 닫힌 일감 상세에 픽업 버튼 없음.
- 되돌리기: scratchpad `close_task_sql.py restore`(닫기 직전 6건의 상태를 스크립트에 적어 뒀다).

### 0040 재실측 — 이 단계의 목적

| 역할 | 닫기 전 | 닫은 뒤 |
|---|---|---|
| auditor | 206 | **170** |
| auditor2 | 206 | **193** |
| auditor3 | 206 | **194** |
| admin | 218 | 218 |
| owner / owner2 | 57 / 53 | 57 / 53 |
| anon | 0 | 0 |

세무사마다 다른 것은 픽업량이 달라서다. ②(열린 일감)가 빠지면 남는 것은 사실상 ①뿐이고,
이는 0040 때 계산해 둔 "②를 빼도 170~194건"과 정확히 일치한다.

## 검증

- 새 `e2e_close2.mjs` **18/18** — 버튼 라벨의 건수 · 경고 문구 · 새로고침 없이 전부 종료 · 버튼 사라짐 · 새로고침 유지 · DB 대조 ·
  **과거마감 일감 2개를 만들어 절차가 반복되는지** · 일감 상세 픽업·초안 유지 · 412 가로 스크롤 0.
- 새 `e2e_close3.mjs` **10/10** — 세무사 진행중 70행·완료 91행 유지 · 닫힌 일감의 내 검수 건 워크스페이스 전사 열림 · 큐 안내 · 픽업 버튼 없음.
- 회귀 `regress.mjs LIVE=1` **52/52**, 본문 차이 7개는 전부 의도한 것(상태 라벨 "진행중"(3) → "종료"(2) 로 1자씩, `/admin/tasks` 646 → 638).
  **새 회귀 기준 = `regress_post3.json`.** 사장님 8화면 8/8.
- (a) 21/21 · (b) 30/30 · (c) 44/44 · (d) 32/32 · (e) 41/41 · 후속2 40/40 · sync-retry 2/2 ·
  DB `test_0040 --applied` 52/52 · 0039 76/76 · 0038 77/77 · 0037 87/87 · tsc 0 · lint 41 → 41 · build ✓.
- E2E 데이터 원복 — 대화 218 / 신청 0 / 메일 1 / chat_turns 408 / 검수 557 / 방·메시지·제안·동의·열람 0.
  **새 기준값: 일감 6 = 전부 `closed`.**

## E2E 가 잡은 것 (전부 스크립트 쪽 — 제품 결함 0)

- 상태 라벨을 "마감"으로 기대했는데 `TASK_STATUS_LABEL.closed` 는 **"종료"** — 1회차에서 헛FAIL 6개.
- `reload()` 직후 표를 읽으면 hydration 전이라 `{}` → 행 수가 맞을 때까지 재시도.
- **세무사 화면을 사이드바 텍스트로 확인해 "로딩 중…" 화면을 통과시켰다**(2회차). "로딩 중" 이 사라질 때까지 기다리는 `bodyReady` 로 고쳤다.
- `regress.mjs` 의 `ONLY=` 는 **경로가 아니라 역할** 필터다 — `ONLY=/audit/dashboard` 는 0화면을 돌고 조용히 통과한다.
- `regress_room.py make` 가 찍어 주는 방 id 를 `OWNER_ROOM` 에 넣어야 한다 — `OWNER_ROOM=1` 은 `/rooms/1` 을 열어 헛FAIL.

## 남은 것

- `ONLY=auditor` 로 세무사 32화면만 연속으로 돌리면 데스크톱 첫 화면 하나가 "로딩 중…"으로 남는다
  (1회차 `/audit/dashboard`, 2회차 `/audit/results` — **화면이 옮겨 다니므로 화면 결함이 아니라 적재 경합**). 52화면 전체 회귀에서는 둘 다 정상.
- 자동 종료(pg_cron)는 만들지 않았다 — 운영자가 버튼으로 고른다는 결정.
- 다음(마지막) 단계: **#2 컬렉션을 채널 하나로 묶기** — 지시서 `design/다음세션_프롬프트_상담사채팅후속4.md`.
